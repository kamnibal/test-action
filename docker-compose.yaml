services:
  mi-web:
    # Apunta a la etiqueta 'latest', que se actualiza en cada push a 'main'
    image: ghcr.io/kamnibal/mi-pagina-web:main
    
    container_name: mi-pagina-web-container

    ports:
      - "8080:80"
      
    restart: always